{
  "name": "streamglass",
  "version": "1.0.0",
  "description": "Streaming utilities for Twitch/OBS",
  "main": "index.js",
  "scripts": {
    "test-coverage": "npm run test-coverage --workspaces",
    "make-badges": "npm run make-badges --workspaces",
    "concat-md-docs": "npm run concat-md --workspace=utilities --",
    "generate-docs": "npm run test-coverage --workspaces && npm run make-badges && npm run concat-md-docs"
  },
  "author": "Giuseppe Giulio Caponetto",
  "license": "MIT",
  "workspaces": [
    "packages/frontend",
    "packages/server",
    "packages/utilities"
  ],
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.8",
    "concat-md": "^0.5.1",
    "vitest": "^3.0.7"
  }
}
